PORT=3100
AUTH_SECRET=           # openssl rand -hex 32
RESEND_API_KEY=        # from resend.com
BASE_URL=https://reader.jfdi.bot
ALLOWED_EMAILS=        # comma-separated list of allowed login emails
FROM_EMAIL=            # e.g. "JFDI Reader <reader@indyhall.org>"
